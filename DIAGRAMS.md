# 🎨 VISUAL GUIDES & DIAGRAMS

Visual representations of how your DiffChecker works!

---

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                        USER BROWSER                          │
│                     http://localhost:3000                    │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ HTTP Requests
                         │
┌────────────────────────▼────────────────────────────────────┐
│                    NEXT.JS FRONTEND                          │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Pages                    Components                 │  │
│  │  • Home (/)               • Navbar                   │  │
│  │  • Saved (/saved/[id])    • Editor (Monaco)          │  │
│  │                           • DiffResult               │  │
│  │                           • SaveModal                │  │
│  └──────────────────────────────────────────────────────┘  │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ axios API calls
                         │
┌────────────────────────▼────────────────────────────────────┐
│                 EXPRESS.JS BACKEND                           │
│                  http://localhost:5000                       │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Routes          Controllers         Models          │  │
│  │  /api/diff  →    compareDiff    →   Comparison      │  │
│  │  /api/save  →    saveComparison      (Schema)       │  │
│  │  /api/view  →    getComparison                      │  │
│  └──────────────────────────────────────────────────────┘  │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ mongoose queries
                         │
┌────────────────────────▼────────────────────────────────────┐
│                    MONGODB DATABASE                          │
│               mongodb://localhost:27017                      │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Database: diffchecker                               │  │
│  │  Collection: comparisons                             │  │
│  │  • shareId (indexed)                                 │  │
│  │  • leftText                                          │  │
│  │  • rightText                                         │  │
│  │  • title                                             │  │
│  │  • createdAt (auto-expire after 30 days)            │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow - Compare Text

```
1. USER TYPES TEXT
   ┌─────────────┐
   │ Left Editor │  Hello World
   └─────────────┘
   ┌─────────────┐
   │Right Editor │  Hello Bro
   └─────────────┘
          ↓
2. CLICK "COMPARE NOW"
          ↓
3. FRONTEND sends POST to /api/diff
   {
     leftText: "Hello World",
     rightText: "Hello Bro",
     mode: "lines"
   }
          ↓
4. BACKEND processes with jsdiff
   diffLines(leftText, rightText)
          ↓
5. RETURNS RESULT
   {
     diff: [
       {value: "Hello ", added: false, removed: false},
       {value: "World", removed: true},
       {value: "Bro", added: true}
     ],
     stats: {added: 1, removed: 1, unchanged: 1}
   }
          ↓
6. FRONTEND DISPLAYS
   ┌─────────────────────┐
   │  Hello              │  (unchanged)
   │  World  (red)       │  (removed)
   │  Bro    (green)     │  (added)
   └─────────────────────┘
```

---

## 💾 Data Flow - Save Comparison

```
1. USER CLICKS "SAVE & SHARE"
          ↓
2. MODAL OPENS
   ┌──────────────────────┐
   │ Title: My Comparison │
   │ Language: javascript │
   │ [Save & Get Link]    │
   └──────────────────────┘
          ↓
3. FRONTEND sends POST to /api/save
   {
     leftText: "...",
     rightText: "...",
     title: "My Comparison",
     language: "javascript"
   }
          ↓
4. BACKEND PROCESSES
   • Generate unique ID (nanoid)
   • Save to MongoDB
   • Return share URL
          ↓
5. MONGODB SAVES
   {
     shareId: "abc123XYZ",
     leftText: "...",
     rightText: "...",
     title: "My Comparison",
     language: "javascript",
     createdAt: "2025-12-09T..."
   }
          ↓
6. RETURNS SHARE URL
   {
     shareId: "abc123XYZ",
     shareUrl: "http://localhost:3000/saved/abc123XYZ"
   }
          ↓
7. MODAL SHOWS LINK
   ┌────────────────────────────────────────┐
   │ ✅ Your comparison has been saved!     │
   │                                        │
   │ http://localhost:3000/saved/abc123XYZ  │
   │                                        │
   │ [Copy Link]  [Close]                   │
   └────────────────────────────────────────┘
```

---

## 📖 Data Flow - View Saved Comparison

```
1. USER OPENS SHARED LINK
   http://localhost:3000/saved/abc123XYZ
          ↓
2. FRONTEND EXTRACTS ID
   params.id = "abc123XYZ"
          ↓
3. FRONTEND sends GET to /api/view/abc123XYZ
          ↓
4. BACKEND QUERIES MONGODB
   Comparison.findOne({shareId: "abc123XYZ"})
          ↓
5. MONGODB RETURNS DATA
   {
     leftText: "...",
     rightText: "...",
     title: "My Comparison",
     language: "javascript",
     createdAt: "2025-12-09T..."
   }
          ↓
6. FRONTEND DISPLAYS
   ┌─────────────────────────────────────┐
   │ My Comparison                       │
   │ Created: Dec 9, 2025                │
   │ [Share Link]                        │
   └─────────────────────────────────────┘
   ┌──────────────┐  ┌──────────────┐
   │ Left Text    │  │ Right Text   │
   │ (read-only)  │  │ (read-only)  │
   └──────────────┘  └──────────────┘
```

---

## 🎯 Component Hierarchy

```
App (Root)
│
├── layout.js
│   ├── <html>
│   ├── <body>
│   │   ├── Toaster (notifications)
│   │   └── {children}
│
├── page.js (Home)
│   ├── Navbar
│   ├── Hero Section
│   │   ├── Title (gradient)
│   │   ├── Subtitle
│   │   └── Mode Selector
│   │       ├── Lines Button
│   │       ├── Words Button
│   │       └── Chars Button
│   ├── Editors Grid
│   │   ├── Editor (left)
│   │   │   ├── Header
│   │   │   └── MonacoEditor
│   │   └── Editor (right)
│   │       ├── Header
│   │       └── MonacoEditor
│   ├── Action Buttons
│   │   ├── Compare Button
│   │   ├── Save Button
│   │   └── Clear Button
│   ├── DiffResult (conditional)
│   │   ├── Stats Bar
│   │   │   ├── Added Count
│   │   │   ├── Removed Count
│   │   │   └── Unchanged Count
│   │   └── Diff Display
│   │       ├── Added Lines (green)
│   │       ├── Removed Lines (red)
│   │       └── Unchanged Lines (gray)
│   └── SaveModal (conditional)
│       ├── Form
│       │   ├── Title Input
│       │   └── Language Select
│       ├── Save Button
│       └── Success View
│           ├── Share URL
│           └── Copy Button
│
└── saved/[id]/page.js
    ├── Navbar
    ├── Header
    │   ├── Title
    │   ├── Date
    │   └── Share Button
    └── Editors (read-only)
        ├── Editor (left)
        └── Editor (right)
```

---

## 🔐 API Endpoints Map

```
Backend API (http://localhost:5000)
│
├── /health
│   └── GET - Health check
│       Response: {status: "OK", message: "..."}
│
├── /api
│   │
│   ├── /diff
│   │   └── POST - Compare texts
│   │       Body: {leftText, rightText, mode}
│   │       Response: {diff: [...], stats: {...}}
│   │
│   ├── /save
│   │   └── POST - Save comparison
│   │       Body: {leftText, rightText, title, language}
│   │       Response: {shareId, shareUrl}
│   │
│   ├── /view/:id
│   │   └── GET - Get saved comparison
│   │       Params: id (shareId)
│   │       Response: {leftText, rightText, title, ...}
│   │
│   └── /stats
│       └── GET - Get statistics
│           Response: {total, today}
```

---

## 🎨 UI Layout

```
┌─────────────────────────────────────────────────────────────┐
│  🔷 DiffChecker                     Home    GitHub          │ ← Navbar
├─────────────────────────────────────────────────────────────┤
│                                                             │
│                      DiffChecker                            │ ← Hero
│                  Compare. Analyze. Save. ⚡                 │
│         The most aesthetic diff tool you've ever used       │
│                                                             │
│               [Lines] [Words] [Chars]                       │ ← Mode
│                                                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────────────┐  ┌──────────────────────┐       │
│  │ 📄 Original Text     │  │ ⚡ Modified Text      │       │
│  ├──────────────────────┤  ├──────────────────────┤       │
│  │  1  Hello World      │  │  1  Hello Bro        │       │ ← Editors
│  │  2  Line 2           │  │  2  Line 2 Modified  │       │
│  │  3  Line 3           │  │  3  Line 3           │       │
│  │                      │  │                      │       │
│  └──────────────────────┘  └──────────────────────┘       │
│                                                             │
│   [⚡ Compare Now] [💾 Save & Share] [🗑️ Clear All]        │ ← Actions
│                                                             │
├─────────────────────────────────────────────────────────────┤
│  📊 Stats: 1 Added | 1 Removed | 1 Unchanged               │
│  ┌─────────────────────────────────────────────────────┐  │
│  │     Hello                                            │  │
│  │  -  World                       (red background)     │  │ ← Diff Result
│  │  +  Bro                         (green background)   │  │
│  │     Line 2 Modified                                  │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎯 User Interaction Flow

```
User Journey
│
├── 1. LAND ON HOMEPAGE
│   └── See hero section
│   └── See dual editors
│   └── Choose comparison mode
│
├── 2. ENTER TEXT
│   └── Type in left editor
│   └── Type in right editor
│   └── Or paste content
│
├── 3. COMPARE
│   └── Click "Compare Now"
│   └── See loading state
│   └── View diff results
│   └── Check statistics
│
├── 4. SAVE (Optional)
│   └── Click "Save & Share"
│   └── Enter title (optional)
│   └── Select language
│   └── Click save
│   └── Get share link
│   └── Copy link
│
├── 5. SHARE (Optional)
│   └── Send link to others
│   └── Others open link
│   └── View saved comparison
│   └── Read-only mode
│
└── 6. START OVER
    └── Click "Clear All"
    └── Or click logo to go home
    └── Create new comparison
```

---

## 📊 Database Structure

```
MongoDB: diffchecker
│
└── Collection: comparisons
    │
    ├── Document 1
    │   ├── _id: ObjectId("...")
    │   ├── shareId: "abc123XYZ"  ← Unique, Indexed
    │   ├── leftText: "Hello World"
    │   ├── rightText: "Hello Bro"
    │   ├── title: "My Comparison"
    │   ├── language: "javascript"
    │   ├── createdAt: Date(...)   ← Auto-expire after 30 days
    │   └── updatedAt: Date(...)
    │
    ├── Document 2
    │   ├── ...
    │
    └── Document N
        ├── ...
```

---

## 🚀 Deployment Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                         USERS                                │
└─────────────────┬───────────────────────────────────────────┘
                  │
                  ├─────────────────────┬──────────────────────┐
                  │                     │                      │
         ┌────────▼────────┐   ┌───────▼────────┐   ┌────────▼────────┐
         │                 │   │                │   │                 │
         │  Vercel CDN     │   │  Railway       │   │  MongoDB Atlas  │
         │  (Frontend)     │   │  (Backend)     │   │  (Database)     │
         │                 │   │                │   │                 │
         │  Next.js App    │◄──┤  Express API   │◄──┤  Cloud DB       │
         │  Static Assets  │   │  Node.js       │   │  Replica Set    │
         │                 │   │                │   │                 │
         └─────────────────┘   └────────────────┘   └─────────────────┘
              🌍 Global            🚀 Auto-scale         🔒 Secure
             Edge Network         Load Balanced         Automatic Backup
```

---

## 🎨 Color System

```
Primary Colors:
├── Primary:   #6366f1 ████ (Indigo)
├── Secondary: #8b5cf6 ████ (Purple)
└── Accent:    #ec4899 ████ (Pink)

Status Colors:
├── Success:   #22c55e ████ (Green)
├── Error:     #ef4444 ████ (Red)
├── Warning:   #f59e0b ████ (Yellow)
└── Info:      #3b82f6 ████ (Blue)

Background:
├── Darker:    #020617 ████ (Very Dark Blue)
├── Dark:      #0f172a ████ (Dark Blue)
├── Slate:     #1e293b ████ (Slate)
└── Light:     #f1f5f9 ████ (Light Gray)

Diff Colors:
├── Added:     rgba(34, 197, 94, 0.2)  ████ (Green transparent)
├── Removed:   rgba(239, 68, 68, 0.2)  ████ (Red transparent)
└── Unchanged: rgba(100, 116, 139, 0.05) ████ (Gray transparent)
```

---

**Use these diagrams as reference while building! 🎨**
